// ==========================================
//   الدرس الثالث: العمليات (Operators)
// ==========================================

// ╔══════════════════════════════════════╗
// ║   1. العمليات الحسابية               ║
// ║      (Arithmetic Operators)          ║
// ╚══════════════════════════════════════╝

let a = 20;
let b = 7;

console.log("--- العمليات الحسابية ---");
console.log(`${a} + ${b} = ${a + b}`);   // الجمع: 27
console.log(`${a} - ${b} = ${a - b}`);   // الطرح: 13
console.log(`${a} * ${b} = ${a * b}`);   // الضرب: 140
console.log(`${a} / ${b} = ${a / b}`);   // القسمة: 2.857...
console.log(`${a} % ${b} = ${a % b}`);   // باقي القسمة (Modulus): 6
console.log(`${a} ** 2 = ${a ** 2}`);    // الأس (Exponentiation): 400

// باقي القسمة (%) مفيد جداً:
console.log("\nهل 10 زوجي؟", 10 % 2 === 0); // true
console.log("هل 7 زوجي؟", 7 % 2 === 0);    // false


// ╔══════════════════════════════════════╗
// ║   2. عمليات الإسناد                  ║
// ║      (Assignment Operators)          ║
// ╚══════════════════════════════════════╝

console.log("\n--- عمليات الإسناد ---");

let score = 100;
console.log("القيمة الأصلية:", score);

score += 10;  // score = score + 10
console.log("بعد += 10:", score);  // 110

score -= 20;  // score = score - 20
console.log("بعد -= 20:", score);  // 90

score *= 2;   // score = score * 2
console.log("بعد *= 2:", score);   // 180

score /= 3;   // score = score / 3
console.log("بعد /= 3:", score);   // 60

score %= 7;   // score = score % 7
console.log("بعد %= 7:", score);   // 4

score **= 3;  // score = score ** 3
console.log("بعد **= 3:", score);  // 64


// ╔══════════════════════════════════════╗
// ║   3. الزيادة والنقصان               ║
// ║      (Increment & Decrement)         ║
// ╚══════════════════════════════════════╝

console.log("\n--- الزيادة والنقصان ---");

let counter = 5;

// ++ الزيادة بواحد
counter++;
console.log("بعد ++:", counter); // 6

// -- النقصان بواحد
counter--;
console.log("بعد --:", counter); // 5

// الفرق بين البادئة واللاحقة (prefix vs postfix)
let val = 10;

// Postfix: يرجع القيمة القديمة ثم يزيد
console.log("postfix:", val++); // 10 (يطبع القيمة القديمة)
console.log("بعد postfix:", val); // 11

// Prefix: يزيد أولاً ثم يرجع القيمة الجديدة
console.log("prefix:", ++val);  // 12 (يطبع القيمة الجديدة)


// ╔══════════════════════════════════════╗
// ║   4. عمليات المقارنة                 ║
// ║      (Comparison Operators)          ║
// ╚══════════════════════════════════════╝
// ترجع دائماً true أو false

console.log("\n--- عمليات المقارنة ---");

let x = 10;
let y = 5;

console.log(`${x} > ${y}:`, x > y);     // أكبر من: true
console.log(`${x} < ${y}:`, x < y);     // أصغر من: false
console.log(`${x} >= 10:`, x >= 10);    // أكبر أو يساوي: true
console.log(`${x} <= ${y}:`, x <= y);   // أصغر أو يساوي: false

// --- المساواة ---
console.log("\n--- المساواة ---");

// == مساواة غير صارمة (تحوّل النوع تلقائياً) ← تجنبها!
console.log('5 == "5":', 5 == "5");       // true ⚠️ خطير!
console.log("0 == false:", 0 == false);   // true ⚠️
console.log('"" == false:', "" == false); // true ⚠️

// === مساواة صارمة (تقارن القيمة والنوع) ← استخدمها دائماً ✅
console.log('5 === "5":', 5 === "5");       // false ✅
console.log("0 === false:", 0 === false);   // false ✅
console.log('"" === false:', "" === false); // false ✅

console.log("5 === 5:", 5 === 5);           // true ✅

// عدم المساواة
console.log("\n--- عدم المساواة ---");
console.log('5 != "5":', 5 != "5");   // false (غير صارم)
console.log('5 !== "5":', 5 !== "5"); // true (صارم) ✅


// ╔══════════════════════════════════════╗
// ║   5. العمليات المنطقية               ║
// ║      (Logical Operators)             ║
// ╚══════════════════════════════════════╝

console.log("\n--- العمليات المنطقية ---");

// --- && (AND) - "و" ---
// true فقط إذا كل الشروط صحيحة
console.log("true && true:", true && true);     // true
console.log("true && false:", true && false);   // false
console.log("false && true:", false && true);   // false
console.log("false && false:", false && false); // false

let userAge = 25;
let hasLicense = true;
console.log(`عمره ${userAge} وعنده رخصة: ${userAge >= 18 && hasLicense}`); // true

// --- || (OR) - "أو" ---
// true إذا واحد من الشروط على الأقل صحيح
console.log("\ntrue || false:", true || false);   // true
console.log("false || true:", false || true);     // true
console.log("false || false:", false || false);   // false

let isWeekend = true;
let isHoliday = false;
console.log(`عطلة؟ ${isWeekend || isHoliday}`); // true

// --- ! (NOT) - "ليس" ---
// يعكس القيمة
console.log("\n!true:", !true);   // false
console.log("!false:", !false);   // true
console.log("!0:", !0);           // true (0 = falsy)
console.log('!"مرحباً":', !"مرحباً"); // false (نص مش فاضي = truthy)

// !! تحويل أي قيمة إلى Boolean
console.log('!!"نص":', !!"نص");   // true
console.log("!!0:", !!0);         // false


// ╔══════════════════════════════════════╗
// ║   6. عمليات النصوص                   ║
// ║      (String Operators)              ║
// ╚══════════════════════════════════════╝

console.log("\n--- عمليات النصوص ---");

// + يدمج النصوص (Concatenation)
let firstName = "غيث";
let lastName = "أحمد";
let fullName = firstName + " " + lastName;
console.log(fullName); // "غيث أحمد"

// += يضيف نص على نص موجود
let sentence = "أنا أتعلم";
sentence += " JavaScript";
sentence += " وهي رائعة!";
console.log(sentence);

// تحذير: الجمع مع نصوص يحوّل الأرقام لنصوص!
console.log("5" + 3);    // "53" (نص!) ← + مع نص = دمج
console.log("5" - 3);    // 2 (رقم!)  ← - يحوّل لرقم
console.log("5" * 2);    // 10 (رقم!)
console.log("5" / 2);    // 2.5 (رقم!)


// ╔══════════════════════════════════════╗
// ║   7. العامل الثلاثي                  ║
// ║      (Ternary Operator)              ║
// ╚══════════════════════════════════════╝

console.log("\n--- العامل الثلاثي ---");

// الصيغة: شرط ? قيمة_إذا_صح : قيمة_إذا_غلط

let age = 20;
let status = age >= 18 ? "بالغ" : "قاصر";
console.log(`العمر ${age}: ${status}`); // "بالغ"

let temperature = 35;
let weather = temperature > 30 ? "حار 🔥" : "معتدل 🌤️";
console.log(`الحرارة ${temperature}°: ${weather}`);

let grade = 85;
let result = grade >= 90 ? "ممتاز" :
             grade >= 80 ? "جيد جداً" :
             grade >= 70 ? "جيد" :
             grade >= 60 ? "مقبول" : "راسب";
console.log(`العلامة ${grade}: ${result}`);


// ╔══════════════════════════════════════╗
// ║   8. Nullish Coalescing (??)         ║
// ║   و Optional Chaining (?.)           ║
// ╚══════════════════════════════════════╝

console.log("\n--- Nullish Coalescing & Optional Chaining ---");

// ?? يرجع القيمة اليمنى فقط إذا اليسرى null أو undefined
let userName = null;
console.log(userName ?? "زائر");  // "زائر"

let userScore = 0;
console.log(userScore ?? 100);   // 0 (لأن 0 مش null/undefined)
console.log(userScore || 100);   // 100 (|| يعتبر 0 falsy!) ← فرق مهم!

// ?. يتحقق إذا الشيء موجود قبل ما يكمل
let user = { name: "غيث", address: null };
console.log(user?.name);            // "غيث"
console.log(user?.address?.city);   // undefined (بدون خطأ!)
// console.log(user.address.city);  // ❌ خطأ! TypeError


// ╔══════════════════════════════════════╗
// ║   9. أولوية العمليات                 ║
// ║      (Operator Precedence)           ║
// ╚══════════════════════════════════════╝

console.log("\n--- أولوية العمليات ---");

// الأولوية من الأعلى للأدنى:
// 1. () الأقواس
// 2. ** الأس
// 3. * / % الضرب والقسمة
// 4. + - الجمع والطرح
// 5. < > <= >= المقارنة
// 6. === !== المساواة
// 7. && AND
// 8. || OR
// 9. = += -= الإسناد

console.log(2 + 3 * 4);       // 14 (الضرب أولاً)
console.log((2 + 3) * 4);     // 20 (الأقواس أولاً)
console.log(2 ** 3 ** 2);     // 512 (الأس من اليمين: 3**2=9, 2**9=512)
console.log(true || false && false); // true (&& قبل ||)


// ╔══════════════════════════════════════╗
// ║   🎯 تمارين                          ║
// ╚══════════════════════════════════════╝

// تمرين 1: تحقق إذا رقم زوجي أو فردي
let num = 15;
console.log(`\nالرقم ${num} هو: ${num % 2 === 0 ? "زوجي" : "فردي"}`);

// تمرين 2: احسب مساحة مستطيل (الطول × العرض)
let length = 10;
let width = 5;
let area = length * width;
console.log(`مساحة المستطيل: ${length} × ${width} = ${area}`);

// تمرين 3: حوّل درجة الحرارة من فهرنهايت لسيلسيوس
// الصيغة: (F - 32) × 5/9
let fahrenheit = 98.6;
let celsius = ((fahrenheit - 32) * 5) / 9;
console.log(`${fahrenheit}°F = ${celsius.toFixed(1)}°C`);

// تمرين 4: تحقق إذا شخص يقدر يصوت (عمره >= 18 ومواطن)
let personAge = 20;
let isCitizen = true;
let canVote = personAge >= 18 && isCitizen;
console.log(`يقدر يصوت؟ ${canVote}`);

// تمرين 5: استخدم العامل الثلاثي لتحديد وقت اليوم
let hour = 14;
let timeOfDay = hour < 12 ? "صباح ☀️" :
                hour < 17 ? "ظهر 🌤️" :
                hour < 21 ? "مساء 🌅" : "ليل 🌙";
console.log(`الساعة ${hour}: ${timeOfDay}`);

console.log("\n✅ انتهى الدرس الثالث - العمليات (Operators)");
console.log("================================================");
