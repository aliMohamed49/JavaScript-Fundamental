// ================================================
//   الدرس الثاني: المتغيرات وأنواع البيانات
// ================================================

// ╔══════════════════════════════════════╗
// ║   شو هو المتغير (Variable)؟        ║
// ╚══════════════════════════════════════╝
// المتغير هو "صندوق" بنخزن فيه بيانات (قيمة) ونعطيه اسم
// مثل: صندوق اسمه "age" فيه القيمة 25


// ╔══════════════════════════════════════╗
// ║   1. طرق تعريف المتغيرات           ║
// ╚══════════════════════════════════════╝

// --- let: متغير قابل للتغيير (الأكثر استخداماً) ---
let age = 25;
console.log("العمر:", age);

age = 26; // ✅ يمكن تغيير القيمة
console.log("العمر بعد التعديل:", age);

// --- const: ثابت لا يمكن تغييره ---
const PI = 3.14159;
console.log("قيمة PI:", PI);

// PI = 3.14; // ❌ خطأ! لا يمكن تغيير قيمة const

const COUNTRY = "Jordan";
console.log("البلد:", COUNTRY);

// --- var: الطريقة القديمة (تجنبها ❌) ---
var oldWay = "هذي الطريقة القديمة";
console.log(oldWay);

// ╔══════════════════════════════════════╗
// ║   الفرق بين let, const, var         ║
// ╚══════════════════════════════════════╝
//
// | الميزة        | let     | const    | var      |
// |--------------|---------|----------|----------|
// | تغيير القيمة  | ✅ نعم  | ❌ لا    | ✅ نعم   |
// | النطاق       | Block   | Block    | Function |
// | إعادة تعريف  | ❌ لا   | ❌ لا    | ✅ نعم   |
// | Hoisting     | ❌      | ❌       | ✅       |
//
// القاعدة: استخدم const دائماً، إلا إذا حبيت تغير القيمة استخدم let


// ╔══════════════════════════════════════╗
// ║   2. قواعد تسمية المتغيرات          ║
// ╚══════════════════════════════════════╝

let firstName = "غيث";      // ✅ camelCase (الأفضل)
let last_name = "أحمد";     // ✅ snake_case (مقبول)
let _private = "خاص";       // ✅ يبدأ بـ _
let $price = 99;            // ✅ يبدأ بـ $
let age2 = 30;              // ✅ يحتوي أرقام (لكن لا يبدأ برقم)

// let 2name = "خطأ";       // ❌ لا يبدأ برقم
// let my-name = "خطأ";     // ❌ لا يحتوي شرطة
// let let = "خطأ";         // ❌ لا يستخدم كلمات محجوزة

console.log(firstName, last_name);


// ╔══════════════════════════════════════╗
// ║   3. أنواع البيانات (Data Types)     ║
// ╚══════════════════════════════════════╝

// JavaScript فيها 7 أنواع بيانات أساسية (Primitive Types):

// --- 1) String (نص) ---
let name = "غيث";
let greeting = 'مرحباً';
let message = `أهلاً يا ${name}`; // Template Literal باستخدام backtick

console.log(name);
console.log(greeting);
console.log(message);

// --- 2) Number (رقم) ---
let integer = 42;          // عدد صحيح
let decimal = 3.14;        // عدد عشري
let negative = -10;        // عدد سالب
let billion = 1e9;         // 1,000,000,000

console.log(integer, decimal, negative, billion);

// أرقام خاصة:
console.log(Infinity);     // ما لا نهاية
console.log(-Infinity);    // سالب ما لا نهاية
console.log(NaN);          // Not a Number - ليس رقم

console.log("نص" * 2);     // NaN - لأنه ما ينفع تضرب نص برقم

// --- 3) Boolean (صح أو غلط) ---
let isStudent = true;
let isGraduated = false;

console.log("طالب؟", isStudent);
console.log("متخرج؟", isGraduated);

// --- 4) Undefined (غير معرّف) ---
let x;
console.log(x);            // undefined - تم تعريف المتغير بدون قيمة

// --- 5) Null (فارغ بقصد) ---
let emptyValue = null;
console.log(emptyValue);   // null - تم تفريغ القيمة بشكل مقصود

// --- 6) BigInt (أرقام كبيرة جداً) ---
let bigNumber = 9007199254740991n;
console.log(bigNumber);

// --- 7) Symbol (معرّف فريد) ---
let id = Symbol("id");
console.log(id);


// ╔══════════════════════════════════════╗
// ║   4. typeof - معرفة نوع البيانات    ║
// ╚══════════════════════════════════════╝
// typeof ترجع نوع القيمة كنص

console.log(typeof "مرحباً");    // "string"
console.log(typeof 42);          // "number"
console.log(typeof true);        // "boolean"
console.log(typeof undefined);   // "undefined"
console.log(typeof null);        // "object" ← خطأ تاريخي بـ JavaScript!
console.log(typeof Symbol("x")); // "symbol"
console.log(typeof 10n);         // "bigint"


// ╔══════════════════════════════════════╗
// ║   5. تحويل أنواع البيانات           ║
// ║      (Type Conversion)              ║
// ╚══════════════════════════════════════╝

// --- تحويل إلى String ---
let num = 123;
let numToString = String(num);
console.log(typeof numToString); // "string"
console.log(numToString);        // "123"

let boolToString = String(true);
console.log(boolToString);       // "true"

// طريقة ثانية: .toString()
let anotherWay = (456).toString();
console.log(anotherWay);         // "456"

// --- تحويل إلى Number ---
let str = "42";
let strToNum = Number(str);
console.log(typeof strToNum);    // "number"
console.log(strToNum);           // 42

console.log(Number("hello"));    // NaN - ما ينفع يتحول
console.log(Number(true));       // 1
console.log(Number(false));      // 0
console.log(Number(""));         // 0
console.log(Number(null));       // 0
console.log(Number(undefined));  // NaN

// طرق أخرى:
console.log(parseInt("50px"));   // 50 - يقرأ الرقم ويتجاهل الباقي
console.log(parseFloat("3.14")); // 3.14

// --- تحويل إلى Boolean ---
console.log(Boolean(1));         // true
console.log(Boolean(0));         // false
console.log(Boolean("مرحباً"));  // true
console.log(Boolean(""));        // false - نص فاضي = false
console.log(Boolean(null));      // false
console.log(Boolean(undefined)); // false

// القاعدة: القيم "الفارغة" تصير false:
// 0, "", null, undefined, NaN → false
// باقي القيم → true


// ╔══════════════════════════════════════╗
// ║   6. Template Literals              ║
// ║   (قوالب النصوص)                    ║
// ╚══════════════════════════════════════╝

let userName = "غيث";
let userAge = 25;

// الطريقة القديمة (String Concatenation)
console.log("اسمي " + userName + " وعمري " + userAge + " سنة");

// الطريقة الحديثة (Template Literal) ← الأفضل ✅
console.log(`اسمي ${userName} وعمري ${userAge} سنة`);

// تقدر تحط تعبيرات (Expressions) داخل ${}
console.log(`بعد 5 سنوات عمري رح يكون ${userAge + 5}`);

// تقدر تكتب على أكثر من سطر بسهولة
let multiLine = `
  السطر الأول
  السطر الثاني
  السطر الثالث
`;
console.log(multiLine);


// ╔══════════════════════════════════════╗
// ║   🎯 تمارين                          ║
// ╚══════════════════════════════════════╝

// تمرين 1: عرّف متغيرات لاسمك، عمرك، وبلدك واطبعهم
let myFullName = "غيث";
let myAge = 25;
const myCountry = "الأردن";
console.log(`اسمي ${myFullName}، عمري ${myAge}، من ${myCountry}`);

// تمرين 2: حوّل النص "100" إلى رقم واجمعه مع 50
let textNumber = "100";
let result = Number(textNumber) + 50;
console.log(`النتيجة: ${result}`); // 150

// تمرين 3: شو نوع كل قيمة؟
console.log(typeof 42);          // ?
console.log(typeof "مرحباً");    // ?
console.log(typeof true);        // ?
console.log(typeof undefined);   // ?
console.log(typeof null);        // ?

// تمرين 4: عرّف ثابت لسعر منتج وحاول غيّره - شو بيصير؟
const productPrice = 29.99;
// productPrice = 39.99; // جرّب شيل التعليق وشوف الخطأ
console.log("سعر المنتج:", productPrice);

console.log("\n✅ انتهى الدرس الثاني - المتغيرات وأنواع البيانات");
console.log("================================================");
